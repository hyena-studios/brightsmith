{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pip show grist > /dev/null 2>&1 || pip install -e ${CLAUDE_PLUGIN_ROOT}",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
